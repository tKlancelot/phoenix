import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(
  // <React.StrictMode>
    //appel du provider de firebase context
        <App/>,
  // </React.StrictMode>,

  document.getElementById('root')
);
